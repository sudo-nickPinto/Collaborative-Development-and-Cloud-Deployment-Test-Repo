# MySQL connection URL used by the Express API.
# Copy this file to .env and replace the example values. The .env file is
# gitignored and should never be committed.
MYSQL_URL=mysql://username:password@localhost:3306/database_name
PORT=3000
