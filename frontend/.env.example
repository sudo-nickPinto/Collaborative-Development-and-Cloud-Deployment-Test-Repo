# Base URL of the backend API (no trailing slash).
# Copy this file to .env.local and fill in a real value - .env.local is
# gitignored, so it stays local to your machine and is never committed.
VITE_API_URL=http://localhost:3000
